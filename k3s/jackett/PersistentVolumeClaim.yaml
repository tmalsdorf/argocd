apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-jackett-config
  namespace: mediamaven
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi  
  volumeName: pv-jackett-config
  storageClassName: ""
